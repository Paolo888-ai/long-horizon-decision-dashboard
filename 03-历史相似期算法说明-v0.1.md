# 历史相似期算法说明 v0.1

## 1. 目的

历史相似期引擎用于回答：当前状态在哪些维度上接近哪些历史时期，当时之后出现过哪些路径，当前与历史之间有哪些不可忽略的结构差异。

它不是“历史复刻预测器”。输出必须保留多案例、多结局和不可比性。

## 2. 输入与输出

### 2.1 输入

- 40 个核心指标的月度特征；
- 六维状态和五周期状态；
- 过去 12、36、60 个月的变化轨迹；
- 制度、人口、技术和地缘结构变量；
- 数据 vintage、质量和缺失标记；
- 结构断裂事件。

### 2.2 输出

每个当前日期、地区和类比类型返回：

- 三至五个非重复历史事件；
- 状态相似度 0～100；
- 轨迹相似度 0～100；
- 结构可比性 0～100；
- 综合可信度；
- 最相似和最不相似的维度；
- 后续 1、3、5 年的结果分布；
- 关键分叉变量；
- 数据不足和结构断裂提示。

## 3. 比较单位

基本单位为“地区—月份”。原始匹配完成后，将相邻月份合并为历史事件窗口。默认：

- 同一国家相距少于 18 个月的候选归为一个事件；
- 每个事件只保留综合分数最高的代表月份；
- 同一榜单至少包含两个不同国家，除非跨国覆盖不足；
- 当前日前 10 年内的候选默认排除，避免把当前同一轮趋势当作历史独立样本；
- 用户可以选择“仅本国历史”取消跨国多样性约束。

## 4. 特征空间

### 4.1 状态特征

使用当前水平和组合关系：

- 六维状态分数；
- 五周期阶段 one-hot/ordinal 编码；
- 关键指标历史分位数；
- 曲线形状，如增长与通胀、信贷与资产的组合；
- 极端事件标签。

### 4.2 轨迹特征

比较过去 36 个月，另以 12 和 60 个月做敏感性测试：

- 标准化指标轨迹；
- 一阶变化方向；
- 转折点相对位置；
- 波动率；
- 维度之间的领先滞后关系。

### 4.3 结构特征

结构可比性不参与寻找“表面最近点”的第一遍搜索，而在第二遍重排：

- 货币制度与央行框架；
- 人口年龄结构；
- 政府和私营部门杠杆结构；
- 全球化和资本流动制度；
- 能源结构；
- 产业构成与技术基础设施；
- 金融监管和社会保障；
- 地缘联盟与冲突状态；
- 数据定义与可得性。

## 5. 数据预处理

1. 仅使用目标历史时点当时已知的数据；
2. 在国家内部进行稳健标准化；
3. 对跨国方向统一，但不强迫绝对水平一致；
4. 对缺失特征不填未来值，只在可比较维度上重标权重；
5. 若有效特征权重低于 60%，候选不可获得中高可信度；
6. 疫情、战争和制度切换通过事件通道处理，不让极值主导所有距离。

## 6. 距离算法

### 6.1 状态距离

对当前时点 t 和历史候选 s：

```text
D_state(t,s) = Σ w_i × q_i × |x_i,t - x_i,s| / Σ有效权重
```

- `w_i`：指标或子主题权重；
- `q_i`：两端数据质量的较小值；
- `x`：映射至 -1～+1 的稳健特征。

将距离按候选历史分布转换为 0～100 相似度，避免固定距离阈值跨数据版本失效。

### 6.2 轨迹距离

基线使用带窗口约束的动态时间规整 DTW，同时设置：

- 最大时间伸缩 20%；
- 方向错误惩罚；
- 转折点错位惩罚；
- 不允许未来区间参与对齐；
- 对 2020 等异常窗口做事件敏感性版本。

另用相关距离和欧氏距离作为基线。DTW 只有样本外表现优于简单方法后才能成为默认。

### 6.3 结构可比性

```text
S_structure = 100 - Σ v_k × mismatch_k
```

`mismatch_k` 为 0、0.5、1，分别表示可比、部分可比、不可比。硬性不同比例超过 50% 时，综合可信度最高只能为“低”。

### 6.4 综合得分

```text
S_total = 0.40 × S_state
        + 0.40 × S_trajectory
        + 0.20 × S_structure
        - break_penalty
```

结构可比性同时作为重排分数和可信度上限，防止数学相似掩盖制度差异。

## 7. 多类型匹配

引擎分别生成五张榜单，不将所有问题混为一体：

| 类型 | 状态 | 轨迹 | 结构 | 主要用途 |
|---|---:|---:|---:|---|
| 综合 | 40% | 40% | 20% | 总体情景 |
| 宏观 | 50% | 35% | 15% | 增长、通胀、就业 |
| 信用资产 | 45% | 40% | 15% | 融资、房地产、金融风险 |
| 技术革命 | 30% | 40% | 30% | 职业、创业、产业方向 |
| 地缘制度 | 30% | 30% | 40% | 供应链、迁移与区域风险 |

## 8. 候选检索与重排

1. 使用降维或近邻索引从所有历史月份取前 100 个状态候选；
2. 计算完整状态和轨迹距离，保留前 30；
3. 计算结构可比性和事件惩罚；
4. 将相邻月份聚为历史事件；
5. 应用国家和结局多样性约束；
6. 选出三至五个案例；
7. 生成证据卡片，所有文案只能引用结构化结果。

## 9. 后续路径统计

### 9.1 结果变量

默认统计历史候选之后 1、3、5 年的：

- 实际 GDP 增长；
- 通胀变化；
- 失业率变化；
- 劳动生产率；
- 实际工资；
- 私营信贷变化；
- 股票、国债和住宅的实际总回报；
- 最大回撤；
- 新产业就业或资本开支扩散。

市场数据如无合法授权，不进入公开版结果。

### 9.2 统计表达

- 展示中位数、25%～75% 区间和全范围；
- 样本少于 10 个不得称为稳定分布；
- 单一案例只展示事实，不展示伪概率；
- 不同国家结果先用实际变化和国内标准分位表达；
- 后续结果与政策干预同时展示，避免把救助后的结果当作自然路径。

## 10. 分叉变量识别

对高相似初始状态但后续结局不同的案例，寻找前 12～24 个月最先分化的变量：

1. 列出结局组间效应量最大的指标；
2. 剔除当时不可观察或发布过晚的变量；
3. 检查变量是否在经济逻辑上可解释；
4. 由规则生成“关键分叉候选”；
5. 研究员仅负责审核表达，不任意增删不支持的结论。

示例：生产率兑现、通胀再加速、贷款标准、企业利润、失业率或政策反应。

## 11. 不可比性与拒绝规则

满足任一条件时拒绝给出高可信历史类比：

- 有效数据权重不足 60%；
- 结构可比性低于 40；
- 前三候选综合得分均低于历史第 70 分位；
- 当前处于训练历史未覆盖的极端区间；
- 重大结构断裂尚未获得足够事后数据；
- 候选全部来自同一事件且无法形成多结局参考。

此时界面显示：“当前组合在已有历史中缺少可靠对应案例。”

## 12. 防泄漏与评估

### 12.1 防泄漏

- 特征只使用当时 vintage；
- 标准化参数只用历史窗口估计；
- 结果变量不参与候选检索；
- 事件严重度只使用当时已知信息；
- 模型调参和最终评估年份分离。

### 12.2 基准

历史引擎至少与以下方法比较：

- 随机历史月份；
- 仅 GDP 和通胀最近邻；
- 仅六维状态最近邻；
- 固定国家内最近邻；
- 无结构重排版本。

### 12.3 评价

- 后续方向命中率；
- 连续结果的 MAE/CRPS；
- 情景概率校准；
- 候选稳定性；
- 对权重和窗口的敏感性；
- 结构可比性是否能减少灾难性误判；
- 用户是否理解“相似不是复刻”。

## 13. 展示规范

每张历史卡片必须同时出现：

1. 历史时期和国家；
2. 三个分数；
3. 三项主要相似；
4. 三项主要差异；
5. 后续路径；
6. 当时关键政策；
7. 当前需监测的分叉变量；
8. 不可直接推论提示。

严禁只展示“相似度 80%”而不解释结构差异。

## 14. v0.1 验收

- 能从给定当前月份返回三个非重复历史事件；
- 能分别生成综合、宏观、信用、技术和地缘榜单；
- 能解释得分的主要贡献和惩罚；
- 缺失数据不会被静默补为零；
- 能拒绝低质量类比；
- 三个手工回测日期不使用未来数据；
- 算法参数、候选结果和最终文案均可追溯。

