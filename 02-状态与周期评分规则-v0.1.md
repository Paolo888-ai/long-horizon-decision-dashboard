# 状态与周期评分规则 v0.1

## 1. 目标

建立一套透明、可复算、可版本化的基线规则，把原始指标转换为：

- 六维环境分数；
- 离散状态标签；
- 五个周期时钟；
- 状态方向、速度与持续性；
- 数据和模型置信度；
- 支持与反对当前判断的证据。

首版不追求最复杂模型，而是建立未来统计模型必须击败的解释型基准。

## 2. 计算层级

```text
原始数据
  → 实时版本筛选
  → 特征转换
  → 单指标信号
  → 子主题分数
  → 六维状态分数
  → 五周期状态
  → 情景输入与个人决策映射
```

## 3. 单指标特征

对指标 i 在月份 t 生成：

| 特征 | 记号 | 默认权重 | 说明 |
|---|---:|---:|---|
| 水平 | L | 35% | 国内历史分位或稳健 z-score |
| 短期动量 | M | 25% | 3 月/月频或 2 季度/季频变化 |
| 中期趋势 | T | 25% | 12 月或 5 年变化，按指标属性选择 |
| 持续性 | P | 15% | 同方向信号持续时间，防止单月噪声 |

单指标原始信号：

```text
raw_signal(i,t) = 0.35L + 0.25M + 0.25T + 0.15P
```

各特征映射至 -100～+100。方向符号由“该指标对所测状态的含义”决定，不将正数统一解释为对用户有利。

## 4. 质量与时效调整

```text
adjusted_signal = raw_signal × quality_weight × freshness_weight
```

### 4.1 质量权重

| 等级 | 权重 |
|---|---:|
| A | 1.00 |
| B | 0.85 |
| C | 0.65 |
| D | 0.40 |

### 4.2 时效权重

```text
freshness = exp(-ln(2) × 超期月数 / 半衰期)
```

默认半衰期：月频 3 个月、季频 6 个月、年度 18 个月。未超过正常发布日的数据权重为 1。

## 5. 六维状态评分

### 5.1 维度内聚合

同一维度先按子主题聚合，避免多个高度相关指标重复计票。例如 CPI、核心 CPI 和 PPI 先形成“价格压力”子主题，不直接作为三个完全独立票数。

```text
dimension_score(d,t) = 加权截尾均值(子主题分数)
```

默认使用 10% 加权截尾均值；指标不足 5 个时改用加权中位数。

### 5.2 六维标签

#### 技术扩散

| 分数与组合 | 状态 |
|---|---|
| 投入低、采用低、生产率低 | 萌芽 |
| 投入快速上升、采用尚低 | 投资爆发 |
| 采用和收入上升、生产率开始改善 | 商业扩散 |
| 广泛采用、生产率和利润兑现 | 全面渗透 |
| 投入高位放缓、回报下降、产能过剩 | 成熟过剩 |

技术阶段采用规则分类器，不仅依赖正负分数。

#### 实体经济

| 分数 | 状态 |
|---:|---|
| ≤ -50 | 深度收缩 |
| -50～-15 | 收缩 |
| -15～+15 且动量转正 | 修复 |
| +15～+50 | 扩张 |
| ≥ +50 且通胀/产能约束升高 | 过热 |

#### 信用金融

| 条件 | 状态 |
|---|---|
| 信贷低位、资产质量改善 | 修复 |
| 信贷脉冲转正、利差收窄 | 扩张 |
| 杠杆和估值高、信贷仍强 | 金融繁荣 |
| 金融条件收紧、曲线倒挂或贷款标准收紧 | 紧缩 |
| 信贷收缩、违约和不良上升 | 去杠杆 |

#### 通胀政策

| 条件 | 状态 |
|---|---|
| 价格和工资持续下降、需求偏弱 | 通缩压力 |
| 通胀接近目标、分项稳定 | 温和 |
| 通胀从低位广泛回升 | 再通胀 |
| 通胀高且实体强 | 过热 |
| 通胀高且实体弱 | 滞胀 |

#### 人口社会

由劳动年龄人口、抚养比、生育、迁移和住房负担共同分类为：红利、平衡、老化、收缩。该维度以 5 年变化为主，月度不因单一数据更新跳变。

#### 地缘制度

依据贸易量、限制事件、供应链压力和地缘风险分类为：开放、竞争、碎片化、冲突。事件型输入必须经规则或人工审核确认。

## 6. 五周期评分

### 6.1 库存周期（1～4 年）

默认权重：

| 因子 | 权重 |
|---|---:|
| PMI 新订单与生产 | 25% |
| 工业生产动量 | 20% |
| 库存—销售缺口 | 30% |
| 企业利润动量 | 15% |
| 信贷短期脉冲 | 10% |

阶段规则：主动补库、被动补库、主动去库、被动去库。阶段必须由库存与需求方向的二维组合确定。

### 6.2 设备与信用周期（7～11 年）

| 因子 | 权重 |
|---|---:|
| 实际资本形成 | 25% |
| 私营信贷缺口 | 20% |
| 信贷脉冲 | 20% |
| 金融条件 | 15% |
| 企业利润率 | 10% |
| 银行资产质量 | 10% |

阶段：修复、扩张、繁荣、紧缩、去杠杆。

### 6.3 人口与建设周期（15～25 年）

| 因子 | 权重 |
|---|---:|
| 劳动年龄人口趋势 | 25% |
| 城市或区域净迁移 | 20% |
| 住房可负担性 | 20% |
| 建筑与基础设施投资 | 20% |
| 家庭形成代理 | 15% |

阶段：人口红利扩张、建设繁荣、供给成熟、人口与资产负担调整。

### 6.4 技术—产业长波（30～60 年）

| 因子 | 权重 |
|---|---:|
| 通用技术投入 | 20% |
| 基础设施资本形成 | 20% |
| 企业采用率 | 20% |
| 生产率兑现 | 20% |
| 新产业利润和就业扩散 | 10% |
| 投资过度与信用压力 | 10% |

输出只使用“萌芽、投资爆发、商业扩散、全面渗透、成熟过剩、调整孕育”，不输出确定性的固定年份。

### 6.5 个人生命周期

该周期不生成宏观分数，而生成约束矩阵：

```text
个人暴露 = 收入暴露 + 资产暴露 + 负债暴露 + 地域暴露 + 时间约束
```

宏观状态与个人暴露相乘后，才允许生成行动优先级。

## 7. 方向、速度和持续性

每个维度和周期同时输出：

- 状态：当前在哪一阶段；
- 方向：改善、稳定、恶化或分化；
- 速度：慢、中、快；
- 持续：当前方向已持续多少月；
- 转折概率：未来 6～12 个月改变阶段的基线概率。

方向由 3 个月平滑分数的一阶差分决定，速度由差分在自身历史中的分位数决定。连续两期未满足阈值不得宣布阶段切换；极端事件可以触发例外，但必须留下事件标签。

## 8. 置信度规则

```text
confidence_score =
  25% × 数据覆盖
+ 20% × 数据质量
+ 20% × 指标一致性
+ 15% × 模型一致性
+ 10% × 样本充分度
+ 10% × 参数稳定性
- 结构断裂惩罚
```

| 分数 | 标签 |
|---:|---|
| 0～39 | 低 |
| 40～69 | 中 |
| 70～100 | 高 |

置信度表示对“状态识别”的把握，不表示未来情景发生概率。

## 9. 模型分歧

出现以下情况之一时必须显示分歧：

- 同维度超过 35% 有效权重指向相反状态；
- 规则模型与统计模型相差两个以上阶段；
- 当前分数对任一单指标删除高度敏感；
- 不同标准化窗口导致状态翻转；
- 国家内部与全球传导方向显著相反。

分歧状态不参与生成 C 级不可逆建议。

## 10. 国家与全球聚合

- 所有国家先在自身历史中标准化；
- 全球优先使用真实全球序列；
- 需聚合时采用滚动 GDP 权重，金融指标可采用市场权重作为第二版本；
- 日本利率、政府债务和人口采用专门结构修正规则；
- 中国政策利率采用多利率合成，避免以单一工具代表政策立场；
- 国家状态不因全球状态而被覆盖，两者同时保留。

## 11. 情景概率基线

v0.1 的概率来自历史条件频率与规则校正：

```text
posterior scenario weight ∝
历史相似样本后续频率
× 当前状态匹配
× 结构可比性
× 外生事件修正
```

研究员可以提出概率修正，但必须记录：原始概率、调整后概率、理由、证据、作者和时间。AI 只能解释已计算概率，不得自行生成或修改概率。

## 12. 个人建议门槛

| 建议等级 | 最低状态置信度 | 最低个人信息完整度 | 输出方式 |
|---|---:|---:|---|
| A 可逆 | 低或以上 | 40% | 可主动推荐 |
| B 部分可逆 | 中 | 60% | 条件式建议 |
| C 难逆转 | 高 | 80% | 仅列情景、压力测试和需咨询事项，不下指令 |

## 13. 版本与审计

每月正式发布需冻结：

- 数据 cutoff 时间；
- 原始数据 vintage；
- 特征代码版本；
- 指标权重版本；
- 状态规则版本；
- 人工调整记录；
- 输出快照哈希。

新规则不得覆盖旧结论。历史页面应允许切换“当时模型”与“当前模型重算”，二者必须明显区分。

## 14. v0.1 验收

- 任一维度分数可从原始指标复算；
- 删除单个指标不会在 90% 的历史月份造成两级以上状态跳变；
- 阶段切换具有至少两期确认或事件例外记录；
- 四视角的核心评分覆盖率均达到 70%；
- 置信度与情景概率在界面上不存在含义混淆；
- AI 无法绕过规则直接改变状态或概率。
