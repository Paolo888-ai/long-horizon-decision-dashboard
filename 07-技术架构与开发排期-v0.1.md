# 技术架构与开发排期 v0.1

## 1. 目标与假设

### 1.1 目标

构建可审计的 Web 数据看板，支持中国、美国、日本和全球四个视角，能够：

- 自动或半自动更新 30～50 个指标；
- 保存原始数据及实时 vintage；
- 计算六维状态和五周期时钟；
- 搜索历史相似期；
- 生成多情景与关键分叉；
- 保存不可覆盖的月度预测档案；
- 基于区间化个人画像生成规则约束下的建议。

### 1.2 假设

- 首版为响应式 Web 应用和云端私测版，仅向受邀用户开放；
- 月度正式发布，日频数据只用于月度聚合和监测；
- 初期仅使用官方免费数据，不采购商业数据库；
- MVP 不设用户账户，个人画像仅保存在浏览器本地；
- 第一优先场景为职业与技能选择；
- 北京时间每月 15 日 23:59 为数据 cutoff，20 日发布；
- 情景概率人工调整由模型负责人提出、产品负责人批准；
- 小团队配置：1 名前后端开发、1 名数据/模型开发、产品与研究兼职；
- 先做可在单机和低成本云环境运行的模块化单体，不引入不必要的微服务。

## 2. 推荐架构

```text
官方 API / CSV / XLSX / 手工事件
             │
             ▼
┌──────────────────────────┐
│ 数据采集层               │
│ connectors + scheduler   │
└─────────────┬────────────┘
              ▼
┌──────────────────────────┐
│ 原始与 Vintage 存储      │
│ object storage + metadata│
└─────────────┬────────────┘
              ▼
┌──────────────────────────┐
│ 清洗与特征层             │
│ validation / mapping     │
└─────────────┬────────────┘
              ▼
┌──────────────────────────┐
│ 分析与模型层             │
│ scoring / analog / test  │
└───────┬──────────┬───────┘
        ▼          ▼
┌────────────┐ ┌──────────────┐
│ 发布快照库 │ │ 研究结果库   │
└──────┬─────┘ └──────┬───────┘
       └───────┬──────┘
               ▼
┌──────────────────────────┐
│ API / Web 应用           │
│ dashboard + profile      │
└──────────────────────────┘
```

## 3. 技术选型建议

### 3.1 前端

- Next.js + TypeScript；
- Tailwind CSS 或轻量设计令牌；
- ECharts 作为主图表库；
- 服务端渲染用于公开方法页和快照页；
- 所有预测图必须支持数据来源、版本和无障碍替代表达。

### 3.2 API 与业务层

- Python FastAPI；
- Pydantic 定义数据和输出契约；
- SQLAlchemy/Alembic 管理数据库迁移；
- 个人建议使用确定性规则服务，AI 仅在受限结构化上下文中生成解释。

### 3.3 数据与模型

- Python、Polars/Pandas、NumPy、SciPy、statsmodels、scikit-learn；
- DuckDB 用于本地研究和批量回测；
- PostgreSQL 用于产品元数据、状态和用户数据；
- Parquet 保存原始与特征快照；
- 初期用系统任务或 GitHub Actions/云定时器调度，规模扩大后再评估 Prefect/Dagster。

### 3.4 存储

| 数据 | 推荐存储 |
|---|---|
| 原始 API 响应和文件 | S3 兼容对象存储，按来源/日期/vintage 分区 |
| 标准化观测和特征 | Parquet + PostgreSQL 索引 |
| 指标、来源和模型定义 | PostgreSQL |
| 月度发布快照 | 不可变 JSON + 哈希 + 对象存储 |
| 用户画像 | PostgreSQL，加密敏感字段 |
| 日志和质量告警 | 结构化日志，初期数据库或托管日志服务 |

## 4. 核心数据模型

```text
indicator_definition
source_registry
raw_asset
observation_vintage
feature_monthly
dimension_score
cycle_state
analog_candidate
analog_result
scenario_snapshot
publication_snapshot
model_version
manual_adjustment
user_profile
decision_exposure
quality_issue
```

任何正式结论必须关联 `publication_snapshot_id` 和 `model_version_id`。

## 5. 模块边界

### 5.1 Connector

每个来源独立适配器，统一输出：值、日期、vintage、单位、状态、原始文件哈希。失败不得覆盖上次成功值。

### 5.2 Validator

- 模式验证；
- 日期和频率检查；
- 单位、季调和基期检查；
- 异常跳变；
- 历史修订差异；
- 来源缺失和过期告警。

### 5.3 Feature Engine

只负责确定性转换；参数进入版本库；对任一特征可追溯至原始观测。

### 5.4 Scoring Engine

实现《状态与周期评分规则》。计算结果和解释贡献同时返回，不允许只返回状态标签。

### 5.5 Analog Engine

分离候选检索、轨迹比较、结构重排、事件聚类和结果统计，便于逐层与简单基线比较。

### 5.6 Scenario Engine

历史条件频率生成基线概率；规则修正和人工修正均单独存储。概率必须校验合计为 100%。

### 5.7 Decision Engine

输入宏观状态和区间化画像，输出暴露、行动等级、适用条件和风险。C 级行动永远不输出命令式结论。

### 5.8 Explanation Layer

先由模板生成完整可用解释。若后续加入大模型：

- 只读结构化模型结果；
- 不可访问或修改概率字段；
- 不得创造数据、来源或历史事件；
- 输出经过事实字段校验；
- 记录提示词、模型版本和结果。

## 6. API 草案

```text
GET  /v1/snapshots/latest?geo=CN
GET  /v1/snapshots/{date}?geo=JP
GET  /v1/dimensions/{dimension}?geo=US&date=...
GET  /v1/cycles?geo=GLOBAL&date=...
GET  /v1/analogs?type=technology&geo=CN&date=...
GET  /v1/analogs/{analog_id}
GET  /v1/scenarios?geo=CN&horizon=3y
GET  /v1/indicators/{indicator_id}
GET  /v1/methodology/versions
POST /v1/profile/evaluate
DELETE /v1/profile
```

公开 API 只返回经过授权允许展示的数据；内部研究 API 与公开 API 分离权限。

## 7. 发布流程

```text
采集 → 验证 → 特征计算 → 状态评分 → 历史类比 → 情景生成
 → 自动质量报告 → 研究审核 → 冻结快照 → 发布 → 监测
```

发布前阻断条件：

- 核心数据覆盖低于 70%；
- 关键来源过期；
- 状态对单一指标高度敏感；
- 概率不合计 100%；
- 模型或数据版本缺失；
- 授权状态不允许公开展示。

## 8. 测试策略

| 层级 | 测试 |
|---|---|
| Connector | 固定样例响应、格式变化、限流、断网、重复数据 |
| 数据 | schema、范围、单位、频率、修订、缺失和异常 |
| 特征 | 黄金样例、边界值、无未来数据测试 |
| 评分 | 手工复算、权重和删除敏感性 |
| 历史类比 | 防泄漏、事件去重、拒绝规则和基线比较 |
| API | 契约、权限、缓存、版本一致性 |
| 前端 | 关键流程、无障碍、空状态、低置信状态 |
| 发布 | 快照不可覆盖、回滚、哈希一致性 |

“回滚”仅允许应用和模型回到旧版本；已经发布的历史预测档案不可删除或改写。

## 9. 安全与隐私

- 个人画像默认使用区间，不收集账户明细；
- 画像与分析事件分离标识；
- 服务端密钥存入 secrets manager；
- 所有来源凭据不得进入前端；
- 用户可删除画像；
- 管理端人工概率调整需要双重确认和审计日志；
- 每季度执行依赖、权限和数据泄漏检查。

## 10. 可观测性

必须监测：

- 各来源最后成功时间；
- 指标覆盖和过期率；
- 数据修订幅度；
- 模型运行时间和失败；
- 状态分数异常跳变；
- 相似候选稳定性；
- 发布快照哈希；
- API 错误率和延迟；
- 用户是否误解概率与置信度的研究指标。

## 11. 12 周开发排期

### 第 1～2 周：基础与样例数据

- 确认指标字典和授权优先级；
- 初始化代码库、CI、数据库和对象存储；
- 完成 8～10 个代表指标连接器；
- 建立 observation vintage 和 source registry；
- 将低保真原型转为可点击静态页面。

里程碑：能够显示四视角的样例首页，所有数字标为样例。

### 第 3～4 周：数据管道

- 扩展至 30～40 个指标；
- 完成数据验证、异常、过期和原始文件留存；
- 完成月度特征计算；
- 建立数据与方法页面。

里程碑：至少 32 个指标可重复更新，四视角覆盖率达到 70%。

### 第 5～6 周：评分与周期

- 实现六维评分和五周期规则；
- 输出贡献、反向证据、置信度和模型分歧；
- 完成周期雷达与详情；
- 为三个手工样例建立黄金测试。

里程碑：任一结论均可追溯和手工复算。

### 第 7～8 周：历史镜像

- 实现简单最近邻基线；
- 实现轨迹比较、结构可比性和事件聚类；
- 完成历史镜像列表与详情；
- 加入拒绝规则和后续路径统计。

里程碑：四视角均能返回三个非重复案例或明确拒绝。

### 第 9 周：未来情景

- 建立三期限情景；
- 生成触发、验证和失效条件；
- 实现概率版本与人工调整审计；
- 完成情景页面。

里程碑：概率合计正确，历史和当前版本可复现。

### 第 10 周：个人决策

- 建立区间化用户画像；
- 实现共同风险和建议等级；
- 完成画像删除和隐私流程；
- 验证低置信度下的建议限制。

里程碑：四类决策均能输出条件化且可追溯的建议。

### 第 11 周：预测档案与回测

- 实现月度不可覆盖快照；
- 区分实时预测、事后回测、当前模型重算；
- 完成至少三个历史区间滚动回测；
- 与简单基线比较。

里程碑：首次端到端模拟月度发布成功。

### 第 12 周：质量与试用

- 性能、权限、无障碍和数据授权检查；
- 5～8 名目标用户可用性测试；
- 修复阻断问题；
- 冻结 MVP 模型、数据和产品版本。

里程碑：满足 PRD MVP 验收或形成明确未通过项。

## 12. 人员与现实排期修正

| 配置 | 预估周期 |
|---|---:|
| 1 名全栈独立开发，产品研究兼职 | 16～22 周 |
| 1 名全栈 + 1 名数据/模型 | 12～14 周 |
| 2 名工程 + 1 名数据 + 研究支持 | 9～12 周 |

若中国数据只能人工维护、或商业授权谈判未完成，公开发布可能额外延后 2～6 周，但不应阻塞内部原型。

## 13. 第一开发迭代任务

1. 建立仓库目录和环境；
2. 定义 7 个数据库核心表；
3. 接入 FRED/ALFRED、OECD、e-Stat/BOJ 和国家统计局样例各 2 个指标；
4. 保存原始响应与 vintage；
5. 实现稳健分位数和时效权重；
6. 用静态 JSON 驱动首页原型；
7. 建立日本 1989、美国 2000、中国 2015 三个黄金测试夹具；
8. 生成第一份数据质量报告。

## 14. 开工门槛

- [x] PRD 和七份交付物完成评审；
- [x] 产品名：长期环境决策看板；
- [x] 第一优先场景：职业与技能选择；
- [x] MVP 不设账户，本地保存画像；
- [x] 官方免费数据方案；
- [x] Next.js + FastAPI + PostgreSQL，云端私测；
- [x] 北京时间每月 15 日 23:59 cutoff，20 日发布；
- [x] 模型负责人提出、产品负责人批准概率调整。

开工门槛已满足，可以进入第 13 节第一开发迭代。
